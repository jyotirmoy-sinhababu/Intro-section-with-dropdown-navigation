import React from 'react';

import logo from '../../assests/img/logo.svg';

const Logo = () => {
  return (
    <div>
      <img src={logo} alt='logo is here' className='img' />
    </div>
  );
};

export default Logo;
